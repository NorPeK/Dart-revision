void main() {
  const String name =
      "NorPeK"; // final do the same job except we can send a variable to it only once. meaning that the value can only be known during runtime

  //name = "norpek"; this will give an error
  print(name);
}
